imports:
  - config.yaml

parameters:
  daipeproject:
    databricks:
      address: 'https://adb-8406481409439422.2.azuredatabricks.net'
      repo:
        base_dir: "/Repos/{repo_name}/latest_tag"
      job:
        suffix: "{repo_name}"

  datalakebundle:
    notebook:
      display:
        enabled: False
